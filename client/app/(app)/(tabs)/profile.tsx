import React from 'react';
import { ProfileContainer } from '@/src/features/auth/ui/container/ProfileContainer';

/**
 * 프로필 탭 화면.
 */
export default function ProfileScreen() {
  return <ProfileContainer />;
}
