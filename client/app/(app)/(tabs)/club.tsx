import React from 'react';
import { ClubTabContainer } from '@/src/features/club/ui/container/ClubTabContainer';

/**
 * 클럽 탭 화면.
 * 소속 클럽 여부에 따라 대시보드 or 빈 상태 + 생성·검색 CTA를 표시한다.
 */
export default function ClubScreen() {
  return <ClubTabContainer />;
}
