import React from 'react';
import { VoteListContainer } from '@/src/features/match/ui/container';

/**
 * 투표 탭 — 경기 투표 목록 (다가오는·진행 중·지난 경기).
 */
export default function VoteScreen() {
  return <VoteListContainer />;
}
