import React from 'react';
import { CreateMatchContainer } from '@/src/features/match/ui/container';

/**
 * 경기 투표 등록 (관리자 전용, 모달 스택).
 */
export default function CreateMatchScreen() {
  return <CreateMatchContainer />;
}
